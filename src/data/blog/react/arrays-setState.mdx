---
path: "/blog/react/how-to-update-arrays-with-setState"
date: "2021-01-24"
tags: "Blog"
subtag: "React"
title: "Como actualizar arreglos usando setState"
author: "Ricardoentuiter"
excerpt: "Como actualizar arreglos usando setState"
---

---

Si declaramos un arreglo de la siguiente forma:

```js
this.state = {
	arr: []
}
```

Y después intentamos actualizar el arreglo añadiendo otro elemento de la siguiente forma:

```js
this.setState({
	arr: arr.push(newValue)
}) // Error
```

Nos dirá que **_arr.push_** no es una función, por lo que entonces ¿Cómo podemos modificar nuestro arreglo?

<br />

#### Solución

---

```js
this.setState((prev) => ({
	arr: [...prev.arr, newValue]
}))
```
